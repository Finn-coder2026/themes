/**
 * Forge theme manifest — identity and defaults.
 */

const manifest = {
	name: "forge",
	version: "1.0.0",
	displayName: "Forge",
	tagline: "Clean developer docs",
	defaults: {
		primaryHue: 228,
		defaultTheme: "light",
		fontFamily: "inter",
		accentSaturation: 84,
		accentLightness: 61,
	},
};

export default manifest;
