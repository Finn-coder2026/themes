/**
 * Atlas theme manifest — editorial handbook visual style.
 */

const manifest = {
	name: "atlas",
	version: "1.0.0",
	displayName: "Atlas",
	tagline: "Editorial handbook",
	defaults: {
		primaryHue: 200,
		defaultTheme: "dark",
		fontFamily: "source-serif",
		accentSaturation: 70,
		accentLightness: 56,
	},
};

export default manifest;
